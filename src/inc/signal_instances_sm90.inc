// Auto-generated by tool/generate_instances_sm90.py
// Do not edit by hand.

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCount<6>, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedCooperative, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCount<6>, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedCooperative, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 64, cutlass::gemm::collective::StageCount<4>, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedCooperative, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 64, cutlass::gemm::collective::StageCount<4>, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedCooperative, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 64, cutlass::gemm::collective::StageCount<4>, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedCooperative, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 64, cutlass::gemm::collective::StageCount<4>, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedCooperative, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 64, cutlass::gemm::collective::StageCount<5>, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedPingpong, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 64, cutlass::gemm::collective::StageCount<5>, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedPingpong, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCount<6>, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedPingpong, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCount<7>, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedPingpong, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCount<6>, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedPingpong, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCount<7>, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecializedPingpong, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 64, cutlass::gemm::collective::StageCount<5>, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 64, cutlass::gemm::collective::StageCount<5>, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  64, 256, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 64, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCount<7>, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  128, 256, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 64, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 32, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 64, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_1, cute::_2, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

template void cutlass_gemm_signal_sm90<
  256, 128, 128, cutlass::gemm::collective::StageCountAuto, cute::Shape<cute::_2, cute::_1, cute::_1>, cutlass::gemm::KernelTmaWarpSpecialized, cutlass::epilogue::collective::EpilogueScheduleAuto
>(
  int M, int N, int K,
  int ReLDN,
  int num_segments,
  int* CommThr,
  half* A, half* B, half* D,
  int* MM, int* RA,
  bool Monitor,
  cudaStream_t stream
);

